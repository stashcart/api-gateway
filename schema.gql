# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  authToken: String!
}

type Profile {
  id: ID!
  email: String
  name: String
  phone: String
}

type Product {
  id: Float!
  url: String!
  price: Float
  name: String
  storeName: String!
}

type CartItem {
  id: Float!
  customer: Profile!
  product: Product!
  amount: Float!
  status: CartItemStatus!
}

enum CartItemStatus {
  PENDING
  APPROVED
  REJECTED
}

type CartPreview {
  id: Float!
  title: String!
  owner: Profile!
  storeName: String!
  isAutoApproveEnabled: Boolean!
}

type Cart {
  id: Float!
  title: String!
  owner: Profile!
  storeName: String!
  isAutoApproveEnabled: Boolean!
  items: [CartItem!]!
}

type Store {
  id: Float!
  name: String!
  url: String!
}

type Query {
  login(email: String!, password: String!): AuthResponse!
  profile(id: String!): Profile!
  myProfile: Profile!
  carts: [CartPreview!]!
  cart(itemsStatus: CartItemStatus, cartId: Float!): Cart!
  stores: [Store!]!
}

type Mutation {
  register(registerInput: RegisterInput!): AuthResponse!
  patchMyProfile(patchProfileInput: PatchProfileInput!): Profile!
}

input RegisterInput {
  email: String!
  password: String!
}

input PatchProfileInput {
  name: String
  email: String
  phone: String
}
